var a: int;
var b: int[10];
b[0] := 0; 
b[1] := 1; 
if b[0] <= b[1] -> 
    write "good"; 
    if b[1] > a ->
        write "bad";
	if b ->
           write a;
        [] else ->
           writes "here";
        fi
    fi
fi
