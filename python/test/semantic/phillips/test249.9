# override int, bool & str in a proc						succeed

proc evil()
	type temp = int;
	type int = bool;
	type bool = string;
	type string = temp;
	
	var b : int;
	var i : string;
	var s : bool;
		
	?b;
	i + 0;
	int(s);

end

evil();
