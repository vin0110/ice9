#Test case: 8:2


var a: int;

proc check(a: int, bo: bool, st: string) : int
    var b: int; 
    fa i := 1 to 10 -> 
        write b + a; 
    af 
    check = a; 
end

forward check (a: int, bo: bool, st: string) : int

a := check(100,true,"Hello");
