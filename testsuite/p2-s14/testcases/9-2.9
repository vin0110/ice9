#Test case: 9-1.9
forward check (a: int, bo: bool, st: string);

proc check(a: int, bo: bool, st: string) 
    var b: int; 
    fa i := 1 to 10 -> 
        write b + a; 
	return;;
    af 
    exit; 
end
check(100,true,"Hello");
